import { grey900, white } from 'material-ui/styles/colors';

const styles = {
  mainContainer: {
    alignItems: 'center',
    backgroundColor: white,
    marginLeft: 'auto',
    marginRight: 'auto',
    marginTop: 20,
    width: '100%',
  },
  title: {
    color: grey900,
    fontSize: 18,
    textAlign: 'center',
    whiteSpace: 'normal',
  },
};

export default styles;
