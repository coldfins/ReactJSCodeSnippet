import { red600, transparent } from 'material-ui/styles/colors';

const styles = {
  container: {
    backgroundColor: transparent,
    paddingTop: 10,
    textAlign: 'center',
  },
  message: {
    color: red600,
    fontSize: 14,
    wordWrap: 'break-word',
  },
};

export default styles;
